claude plugin marketplace update && claude plugin uninstall doarakko-config@doarakko-config && claude plugin install doarakko-config@doarakko-config をzshrcに追加しろ

---

[Request interrupted by user for tool use]

---

それ

---

[Request interrupted by user]

---

どうやって実行するつもりだ？

---

このリポジトリのを更新しろ

---

このリポジトリのファイルを更新するように claude.mdを更新しリオ

---

全ての場合において

---

# コミットコマンド

現在の変更をコミットし、リモートにプッシュする。

## 使用方法
```
/commit
```

## 手順
1. `git status` で変更を確認
2. Conventional Commits形式でコミットメッセージ作成
3. `git push` でリモートに反映

## Conventional Commits形式
- `type(scope): subject`
- タイトルは50文字以内、本文は72文字で改行
- 動詞は原形（add, fix, update等）
- scopeは原則記述、適切なものがなければ省略可
- 小文字で始める
- 実装とテストが含まれる場合、typeはfeat/fixを優先

## 絶対に守るべきルール
- 関係のない変更はコミットに含めない
- 関係のない変更を消さない
- `git checkout`、`git restore`、`git reset`、`git stash` は使わない