# AGENTS.md

このリポジトリの規約。ローカルの`codex exec`とGitHub上のコードレビューが直接読み、Claude Codeは`CLAUDE.md`のインポート経由で読む。

規約はこのファイルにだけ書く。`CLAUDE.md`へ規約を直接書き足さないこと。

## 前提

Claude Codeの設定（コマンド・スキル・エージェント・hook）を配布するプラグインリポジトリ。ホームディレクトリへはプラグインマーケットプレイス経由でコピーされる。

## ルール

- Claude Codeに関する変更を加える場合は、憶測で判断せず、必ず公式のドキュメントと公式プラグインを確認してから行う
- ファイルを変更する場合は、常にこのリポジトリ内のファイルを更新する。ホームディレクトリや他の場所にあるファイルを直接編集してはならない
- コードを追加・変更した場合は、関連するドキュメント（README.md、docs/配下など）も合わせて更新する
- コード内のコメントに関数名や変数名は含めない
- 画面に出る変更を含むPRには、動作確認のスクリーンショットや動画を添付する

## Code Review Rules

機械的なチェックではなく、このリポジトリ固有で影響の大きい挙動に絞って指摘すること。

### hookスクリプト

- Stop hookが`stop_hook_active`を確認せずにブロックしていないか。確認を欠くと8連続でブロックした時点で強制終了される
- hookが標準出力に出すJSONが公式のスキーマに沿っているか。`additionalContext`は`hookSpecificOutput`の下に置く必要があり、トップレベルに置くと黙って無視される
- hookの失敗がセッションを止めないこと。副次的な処理（通知など）のエラーは握りつぶす
- ユーザー入力やファイル名をシェルやAppleScriptへ渡す箇所でエスケープが漏れていないか

### コマンドとエージェントの定義

- frontmatterの`allowed-tools`に、手順本文で使うコマンドが漏れなく登録されているか。漏れると実行時に許可プロンプトが出る
- 自動化を意図した手順にAskUserQuestionのような対話が混ざっていないか
- エージェントの`tools`が役割に対して過剰でないか。レビュー系エージェントにWriteやEditを与えない

### E2Eの撮影と添付

- 撮影した成果物をリポジトリ内に書き出していないか。`playwright-cli`の`--filename`は実行時のカレントディレクトリを基準に解決されるため、絶対パスを渡す
- hookとエージェントのBashが共有する一時ファイルを`$TMPDIR`配下に置いていないか。サンドボックス下で両者の指す先が異なる
- 添付の失敗がPR作成そのものを失敗させていないか。作成済みかを確認せずに作り直していないか

### HTMLページの公開

- Artifactへ公開する内容に秘匿情報や絶対パスが混ざっていないか。公開したページはAnthropic側に保存され、ページ上のShareから第三者へ広げられる。絶対パスにはユーザー名が入る
- `Artifact`を`allowed-tools`へ入れていないか。公開はコードをAnthropic側へ上げる操作なので、許可プロンプトを1回残す。`allowed-tools`は制限ではなく事前承認で、入れるとプロンプトごと消える。なお残せるのは初回の1回だけで、承認済みページの再公開とauto modeでは確認が出ない
- 組み込みの`/artifact-pr-review`へ中途半端に委譲していないか。あれはPR番号の起動引数でページの同一性を決め、payload JSONとheadのSHAをpublish時に検証する別系統の手順。委譲するなら起動引数の受け渡し・再公開時のpayload再作成・拒否時のフォールバックまで揃える。揃えられないなら呼ばず、ユーザーが直接実行する選択肢として案内する
- 公開の失敗がレビューやプラン表示そのものを失敗させていないか。Artifactはプランや認証方式によっては使えないため、ローカルHTMLへのフォールバックが要る
- 公開用の一時HTMLをリポジトリ内や`$TMPDIR`配下に書き出していないか。`/tmp/claude/report`配下に置く
- ファイル名を対象ごとに固定しているか。同一セッションでは同じファイルパスへの再公開が同じページの更新になるため、パスが変わると同じレビューでもページが増え、逆にパスが衝突すると別対象のページが差し替わる

### 権限指定

- ファイルパスを絞る指定を`Write(...)`や`Glob(...)`で書いていないか。Claude Codeは`Edit(...)`と`Read(...)`しか照合せず、それ以外はパースするだけで無視して起動時に警告を出す。`Write`ツールのパス判定も`Edit(...)`で書く
- macOSの`/tmp`は`/private/tmp`へのシンボリックリンク。allowルールはリンク自身と解決先の両方が一致したときだけ効くので、`//tmp/...`と`//private/tmp/...`を並べる
- 絶対パスの先頭は`//`。`/`始まりは設定ファイルの位置を起点にした相対指定になる

### 破壊的操作

- `git checkout`、`git restore`、`git reset`、`git stash`をユーザーの明示的な指示なく使っていないか
- 既存ブランチへのpushや、ユーザーの未コミット変更を失わせる操作が含まれていないか

### 配備

- リポジトリ内のファイルだけを変更しているか。ホームディレクトリ配下を直接書き換えていないか
- 実体はプラグインキャッシュへコピーされるため、リポジトリ内のパスを絶対パスで参照していないか。`${CLAUDE_PLUGIN_ROOT}`を使う
- hookからスクリプトを起動するとき、`command`へパスを文字列連結していないか。`command`と`args`に分ける exec 形式ならシェルを経由しないため、パスに空白やクォートが混ざっても壊れない

### サンドボックス設定

`.claude/settings.json`はJSONでコメントを持てないため、消してはいけない理由をここに置く。

- `sandbox.filesystem.allowWrite`の`~/.codex`と、`sandbox.network.allowedDomains`の`chatgpt.com`・`*.openai.com`を外していないか。codexはサンドボックス下でsqlite状態とログを書き、ChatGPTと通信する。書き込み許可が無いと認証確認より前に起動時点で落ちる
- その書き込み許可を絞る`denyWrite`の6パスを外していないか。`config.toml`の`notify`と`mcp_servers.<name>.command`、プラグインの実行ファイル、コマンドを事前承認する execpolicy の`.rules`、skills、bundled marketplace のソース。いずれも**codexが次回サンドボックス外で何を実行するか**を決める。サンドボックス下のコマンドがこれらを書き換えられると、自分自身に非サンドボックス実行を与えられる
- 残る`vendor_imports`は、外部から取得した curated skill のカタログキャッシュ。**改ざんの影響は未検証**だが、書き込みを許す理由も無いので同じく閉じている
- この deny はトレードオフで、無条件に安全な締め付けではない。codexは実行時に`config.toml`を自身で書き換える（モデル移行の通知、プロジェクトごとの`trust_level`）。またこの6つを全て deny した状態で確認できているのはセッションバナーまでで、**レビュー経路全体は未検証**
